services:
  semcom:
    build:
      context: .
      dockerfile: Dockerfile
    image: semcom-playground:dev
    container_name: semcom-playground-dev
    working_dir: /workspace
    volumes:
      - .:/workspace
    command: bash
